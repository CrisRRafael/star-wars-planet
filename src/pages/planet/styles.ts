import styled from "styled-components";

export const PagePlanetDetailsContainer = styled.div`
  .section-go-back {
    display: flex;
    justify-content: flex-end;

    width: 100%;
  }
`;

export const BackButton = styled.button`
  cursor: pointer;

  background: none;
  border: none;

  gap: 8px;
  display: flex;
  align-items: center;

  color: #fff;
  text-align: center;
  font-family: Montserrat;
  font-size: 14px;
  font-style: normal;
  font-weight: 500;
`;
